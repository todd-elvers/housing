#!/usr/bin/env bash
# Convenience wrapper so you can run `./housing <command>` from the repo root.
# Resolves the toolchain through mise (node + aube) regardless of shell activation.
set -euo pipefail
cd "$(dirname "$0")"
exec mise exec -- aube exec tsx src/main.ts "$@"
