# deps
node_modules/
.venv/

# local config / secrets — plaintext .env / .env.local stay local; the
# age-encrypted .env.age and the public .age.public-keys ARE committed.
.env
.env.local
!.env.age
!.age.public-keys

# data + state
data/
*.db
*.db-shm
*.db-wal

# misc
.DS_Store
*.log
scratch/
