# deps
node_modules/
.venv/

# local config / secrets — plaintext .env stays local; the age-encrypted
# .env.age and the .age.public-keys recipients list ARE committed.
.env
!.env.age
!.age.public-keys

# data + state
data/
*.db
*.db-shm
*.db-wal

# misc
.DS_Store
*.log
scratch/
